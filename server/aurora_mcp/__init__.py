"""Aurora MCP package — token-lean hybrid tool surface for external MCP clients.

See `registry.py` for the declarative Tier-1/Tier-2/Tier-3 spec and the
security allowlist (the single source of truth for which agent tools are
reachable from MCP).
"""
